<?xml version="1.0" encoding="UTF-8"?>
<configuration debug="false">

    <!--定义应用名，通常配置在application.properties文件中-->
    <springProperty scop="context" name="app_name" source="spring.application.name" defaultValue="UnknownApp"/>

    <!--定义日志级别和路径，通常配置在apollo，因环境而变-->
    <springProperty scop="context" name="logging_path" source="logging.path" defaultValue="../../logs"/>
    <springProperty scop="context" name="logging_level" source="logging.level" defaultValue="INFO"/>

    <!-- 控制台输出 -->
    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <encoder class="ch.qos.logback.classic.encoder.PatternLayoutEncoder">
            <!--格式化输出：%d表示日期，%thread表示线程名，%-5level：级别从左显示5个字符宽度,%line:行号,%msg：日志消息，%n是换行符-->
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} %-5level %logger{50} line:%line: %msg%n</pattern>
        </encoder>
    </appender>

    <!-- 按照每天生成日志文件 -->
    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${logging_path}/${app_name}/app.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <!--日志文件输出的文件名-->
            <FileNamePattern>${logging_path}/${app_name}/archives/app.%d{yyyy-MM-dd}.log</FileNamePattern>
            <!--日志文件保留天数-->
            <MaxHistory>14</MaxHistory>
        </rollingPolicy>
        <encoder class="ch.qos.logback.classic.encoder.PatternLayoutEncoder">
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} %-5level %logger{50} :%line %msg%n</pattern>
        </encoder>

    </appender>

    <!-- 日志输出位置和级别 -->
    <root level="${logging_level}">
        <appender-ref ref="STDOUT"/>
        <appender-ref ref="FILE"/>
    </root>

</configuration>